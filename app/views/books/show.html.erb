

<div class="alert alert-success">
    <%= flash[:notice] %>
  </div>
<p><span>Title:</span><%= @book.title %></p>
<p><span>Body:</span><%= @book.body %></p>

<p><%= link_to "Edit", edit_book_path(@book.id), class: "edit_#{@book.id}" %> | 
	<%= link_to "Back", books_path, class: "back" %></p>